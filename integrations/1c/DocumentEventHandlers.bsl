// Обработчики подписок ПриЗаписи документов продаж и возвратов.
// Ни одно исключение интеграции не выходит в транзакцию записи документа 1С.

Процедура РасходнаяНакладнаяПослеЗаписи(Источник, Отказ) Экспорт
    // Имя сохранено для совместимости; подписка использует событие ПриЗаписи.
    // Параметр Отказ намеренно не изменяется.
    Попытка
        Настройки = ДХКоверНастройкиСервер.ПолучитьПараметрыПодключения();
        Пакет = ДХКоверПродажиСервер.СформироватьПакетПродажи(
            Источник,
            Настройки.СмещениеЧасовогоПояса);
        ДХКоверТранспортСервер.ОтправитьПакетDhCarpetБезопасно(
            Настройки.АдресСервера,
            Настройки.КлючAPI,
            Пакет,
            Источник.Ссылка);
    Исключение
        ДХКоверТранспортСервер.БезопасноЗаписатьОшибкуСинхронизации(
            Источник.Ссылка,
            ОписаниеОшибки());
    КонецПопытки;
КонецПроцедуры

Процедура ПриходнаяНакладнаяПослеЗаписи(Источник, Отказ) Экспорт
    // Имя сохранено для совместимости; подписка использует событие ПриЗаписи.
    // Параметр Отказ намеренно не изменяется.
    Попытка
        Настройки = ДХКоверНастройкиСервер.ПолучитьПараметрыПодключения();
        Пакет = ДХКоверВозвратыСервер.СформироватьПакетВозвратаПокупателя(
            Источник,
            Настройки.СмещениеЧасовогоПояса);
        ДХКоверТранспортСервер.ОтправитьПакетDhCarpetБезопасно(
            Настройки.АдресСервера,
            Настройки.КлючAPI,
            Пакет,
            Источник.Ссылка);
    Исключение
        ДХКоверТранспортСервер.БезопасноЗаписатьОшибкуСинхронизации(
            Источник.Ссылка,
            ОписаниеОшибки());
    КонецПопытки;
КонецПроцедуры
